{
  "name": "proyecto_bot",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "migrate": "node -e \"const { Sequelize } = require('sequelize'); const path = require('path'); const sequelize = new Sequelize({ dialect: 'sqlite', storage: path.join(__dirname, 'store_bot.db'), logging: console.log }); sequelize.sync({ alter: true }).then(() => { console.log('Base de datos actualizada'); process.exit(0); });\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7"
  }
}
